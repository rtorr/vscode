#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/sh" ]; then
  "$basedir//bin/sh"  "$basedir/../babel/cli.js" "$@"
  ret=$?
else 
  /bin/sh  "$basedir/../babel/cli.js" "$@"
  ret=$?
fi
exit $ret
